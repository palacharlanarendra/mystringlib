//there is no attach that is happpening
//there is no prototype, no window , no global
//


function countWords(givenString){
    // let para = this.toString();
    let splittedPara = givenString.split(" ");
    return splittedPara.length
}


export {countWords};